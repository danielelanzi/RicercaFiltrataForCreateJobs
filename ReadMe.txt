------------------------------------------------------------
RicercaFiltrataForCreateJobs.zip
creato da Daniele Lanzi info: [https://www.linkedin.com/in/danielelanzi/]
per Create Job [https://create-job.com/]
------------------------------------------------------------

Gentile Create Job,

Ho Sviluppato uno script in PHP che permette agli utenti di effettuare ricerche su un Database MySql con dei filtri che sono inseriti in degli input HTML in maniera dinamica, tramite i seguenti input:
    • Input Testuale per la ricerca del nome;
    • Input Select per selezionare l'età definito dal Database
    • Input Select per selezionare la provincia
    • Input Select per il settore lavorativo
    • Input Select per ila posizione lavorativa

L’applicazione è composta con i seguenti file:
    • index.php (Pagina di ricerca del candidato)
    • db_config.php (Pagina cconfigurazione per accesso al Database)
    • database.sql (Database completo di tutte le tabelle per la ricerca)
    • global.js (Codice Bootstrap per lo stile di convalida dei moduli)
    • info.txt (Specifiche in merito alla versione usata di PHP, jQuery o di framework CSS impiegati nello sviluppo)
    • select_print.class.php (File di controller)
    • select_print.php (File di servizio)


grazie per aver esaminato questo file.

